import Image from "next/image"; const logoProperties = { src: "/Logo.svg", alt: "FundX Logo", width: 32, height: 16, }; export default function Logo({ className }: { className?: string }) { return ( <Image {...logoProperties} className={className} /> ); }