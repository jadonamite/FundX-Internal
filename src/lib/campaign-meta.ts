export interface ExtraMeta { creatorName?: string; creatorBio?: string; email?: string; twitter?: string; github?: string; portfolio?: string; projectStage?: string; videoUrl?: string; budgetBreakdown?: string; roadmap?: string; location?: string; } // ─── Client helpers (call the API route) ─────────────────────────── const fetchApi = async (url: string, options?: RequestInit) => { try { const res = await fetch(url, options); if (!res.ok) return null; return res.json(); } catch { return null; } }; export async function fetchExtraMeta(id: number | string): Promise<ExtraMeta | null> { const json = await fetchApi(`/api/campaign-meta?id=${id}`, { cache: "no-store" }); return json?.meta ?? null; } export async function saveExtraMeta(id: number | string, meta: ExtraMeta): Promise<boolean> { const res = await fetchApi(`/api/campaign-meta`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id, meta }), }); return res !== null; }