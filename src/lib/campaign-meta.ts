// Off-chain campaign metadata — the rich fields the create wizard collects that // don't fit the on-chain registry (which stores only title/tagline/description/ // image/category/social). Keyed by the numeric on-chain campaign id. // // Persistence is Upstash Redis via its REST API (no SDK dependency — just fetch). // If the env vars are absent, reads return null and writes no-op, so the app // degrades gracefully to on-chain-only data and the build never breaks. export interface ExtraMeta { creatorName?: string; creatorBio?: string; email?: string; twitter?: string; github?: string; portfolio?: string; projectStage?: string; videoUrl?: string; budgetBreakdown?: string; roadmap?: string; location?: string; } // ─── Client helpers (call the API route) ─────────────────────────── const fetchJson = async (url: string, options?: RequestInit): Promise<any | null> => { try { const res = await fetch(url, options); if (!res.ok) return null; return await res.json(); } catch { return null; } }; export async function fetchExtraMeta(id: number | string): Promise<ExtraMeta | null> { const json = await fetchJson(`/api/campaign-meta?id=${id}`, { cache: "no-store" }); return json?.meta ?? null; } export async function saveExtraMeta(id: number | string, meta: ExtraMeta): Promise<boolean> { const res = await fetchJson(`/api/campaign-meta`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id, meta }), }); return res !== null; }